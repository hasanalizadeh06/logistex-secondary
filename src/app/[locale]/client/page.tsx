import Client from "@/components/pages/client";
import Wrapper from "@/layouts/Wrapper";

export const metadata = {
   title: "Partners ILS - Transport & Logistics",
};
const page = () => {
   return (
      <Wrapper>
         <Client />
      </Wrapper>
   )
}

export default page